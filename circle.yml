machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t registry-docker/nginx-php-7.1 .

test:
  override:
    - docker run -d -p 80:80 registry-docker/nginx-php-7.1; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:80
